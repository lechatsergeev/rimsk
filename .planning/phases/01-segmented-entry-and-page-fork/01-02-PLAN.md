---
phase: 01-segmented-entry-and-page-fork
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/HorecaPage.tsx
  - src/app/RetailPage.tsx
  - src/main.tsx
autonomous: true
requirements:
  - HOR-01
  - HOR-02
  - RET-01
  - RET-02
  - QLT-01
  - QLT-02
user_setup: []
must_haves:
  truths:
    - Horeca-лендинг существует как отдельная страница и продолжает работать
    - Retail-лендинг существует как отдельная страница-копия, готовая к независимой правке
    - Проект собирается и локально запускается после разведения страниц
  artifacts:
    - src/app/HorecaPage.tsx
    - src/app/RetailPage.tsx
    - src/main.tsx
  key_links:
    - src/main.tsx подключает корневой App
    - src/app/App.tsx импортирует HorecaPage и RetailPage
---

<objective>
Выделить horeca и retail в самостоятельные страницы и зафиксировать базовую техническую верификацию.

Purpose: создать независимые точки дальнейшей продуктовой правки без дублирования логики внутри одного огромного файла.
Output: два отдельных page-компонента и подтверждение, что проект собирается и стартует локально.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-segmented-entry-and-page-fork/01-CONTEXT.md
@src/app/HorecaPage.tsx
@src/app/RetailPage.tsx
@src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract horeca page into standalone file</name>
  <files>src/app/HorecaPage.tsx</files>
  <read_first>src/app/HorecaPage.tsx</read_first>
  <action>Убедиться, что horeca-лендинг живёт в отдельном файле `src/app/HorecaPage.tsx` и экспортируется как самостоятельный page-компонент без привязки к старому монолитному `App.tsx`.</action>
  <verify>rg -n "export default function App|export default function HorecaPage|Hero" src/app/HorecaPage.tsx</verify>
  <acceptance_criteria>
    - Horeca-страница существует отдельным файлом
    - Компонент можно импортировать в корневой App как отдельную страницу
  </acceptance_criteria>
  <done>Horeca вынесена в отдельный самостоятельный page-компонент.</done>
</task>

<task type="auto">
  <name>Task 2: Create retail clone and verify runtime</name>
  <files>src/app/RetailPage.tsx, src/main.tsx</files>
  <read_first>src/app/RetailPage.tsx, src/main.tsx</read_first>
  <action>Создать `src/app/RetailPage.tsx` как отдельную копию horeca-страницы для будущего retail-редактирования и убедиться, что bootstrap приложения остаётся корректным. После этого проверить проект через `npm run build` и локальный dev-сервер.</action>
  <verify>npm run build</verify>
  <acceptance_criteria>
    - `src/app/RetailPage.tsx` существует как отдельный page-файл
    - `npm run build` завершается успешно
    - Локальный сервер поднимается и позволяет открыть сегментный экран
  </acceptance_criteria>
  <done>Retail-копия создана, а проект проходит базовую техническую проверку.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] Локальный сервер доступен для ручной проверки
- [ ] Horeca и retail существуют как разные файлы
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Кодовая база готова к отдельной retail-адаптации во Phase 2
</success_criteria>

<output>
After completion, create `.planning/phases/01-segmented-entry-and-page-fork/01-02-SUMMARY.md`
</output>
