---
phase: 01-segmented-entry-and-page-fork
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/App.tsx
autonomous: true
requirements:
  - SEG-01
  - SEG-02
  - SEG-03
user_setup: []
must_haves:
  truths:
    - Пользователь сначала видит экран выбора сегмента, а не сразу horeca-лендинг
    - Выбор horeca и retail переключает на соответствующую ветку контента
    - Пользователь может вернуться к экрану выбора и сменить сегмент
  artifacts:
    - src/app/App.tsx
  key_links:
    - src/app/App.tsx подключает и переключает horeca/retail страницы
---

<objective>
Собрать точку входа с сегментным выбором и переключением между ветками.

Purpose: убрать смешение аудиторий уже на первом экране.
Output: обновлённый `src/app/App.tsx` с экраном выбора, переключателем и связкой со страницами сегментов.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-segmented-entry-and-page-fork/01-CONTEXT.md
@src/app/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build segment entry screen</name>
  <files>src/app/App.tsx</files>
  <read_first>.planning/phases/01-segmented-entry-and-page-fork/01-CONTEXT.md, src/app/App.tsx</read_first>
  <action>Создать в `src/app/App.tsx` отдельный первый экран с вопросом "Вы из хорека или из ретейла?" и двумя явными карточками выбора. Экран должен визуально жить до основного контента, а карточки должны вызывать выбор сегмента без использования отдельного роутера.</action>
  <verify>rg -n "Вы из хорека|retail|horeca" src/app/App.tsx</verify>
  <acceptance_criteria>
    - `src/app/App.tsx` содержит первый экран с двумя сегментами
    - В коде явно присутствуют оба состояния: `horeca` и `retail`
  </acceptance_criteria>
  <done>Первый экран выбора сегмента реализован и читается из кода без двусмысленности.</done>
</task>

<task type="auto">
  <name>Task 2: Add active segment switching and reset path</name>
  <files>src/app/App.tsx</files>
  <read_first>src/app/App.tsx</read_first>
  <action>Добавить управление активным сегментом, отрисовку нужной страницы после выбора и отдельное действие для возврата к выбору. Сохранять сегмент в URL-параметре допустимо, если это упрощает проверку и переключение.</action>
  <verify>rg -n "segment|К выбору|handleReset|handleSelect" src/app/App.tsx</verify>
  <acceptance_criteria>
    - Пользовательский выбор управляет рендерингом активной страницы
    - В коде есть явный способ сбросить выбор сегмента
  </acceptance_criteria>
  <done>Сегмент выбирается, переключается и сбрасывается без отдельного роутера.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build`
- [ ] Проверка, что `src/app/App.tsx` рендерит экран выбора и активную ветку
- [ ] Нет поломки horeca-потока после сегментации
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Первый экран и переключение сегментов работают как единая точка входа
</success_criteria>

<output>
After completion, create `.planning/phases/01-segmented-entry-and-page-fork/01-01-SUMMARY.md`
</output>
